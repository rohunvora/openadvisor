{
  "name": "openadvisor",
  "version": "1.0.0",
  "description": "Transparency-first platform for crypto KOL token grants on Solana",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "contracts/*"
  ],
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run -r build",
    "test": "pnpm run -r test",
    "lint": "pnpm run -r lint",
    "clean": "pnpm run -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["solana", "crypto", "kol", "transparency", "web3"],
  "author": "OpenAdvisor Team",
  "license": "MIT"
}
